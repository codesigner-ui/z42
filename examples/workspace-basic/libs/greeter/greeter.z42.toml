# lib member: 提供 Greeter.greet() 函数。
# 通过 .workspace = true 引用共享元数据。

[project]
name              = "greeter"
kind              = "lib"
version.workspace = true
license.workspace = true
authors.workspace = true

[sources]
include = ["src/**/*.z42"]
