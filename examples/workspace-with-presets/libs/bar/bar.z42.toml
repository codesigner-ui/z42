# 同时 include lib-defaults + strict-lints：strict-lints 后写，覆盖 lib-defaults 中重叠字段。
include = [
    "${workspace_dir}/presets/lib-defaults.toml",
    "${workspace_dir}/presets/strict-lints.toml",
]

[project]
name              = "bar"
version.workspace = true
license.workspace = true
