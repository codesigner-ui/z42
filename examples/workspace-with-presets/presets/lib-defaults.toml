# 共享 lib 默认值：所有 libs/* 的 member 通过 include 拉入。
# preset 不能含 [project].name / entry / [workspace.*] / [policy] / [profile.*]（WS021）。

[project]
kind = "lib"

[sources]
include = ["src/**/*.z42"]
exclude = ["src/**/*_test.z42"]

[build]
incremental = true
