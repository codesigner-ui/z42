[package]
name = "z42-abi"
version.workspace = true
edition = "2021"
description = "z42 Tier 1 native interop ABI — stable C ABI mirror types"
license = "MIT OR Apache-2.0"

[lib]
path = "src/lib.rs"
