void Main() {
    var x = 10;
    var msg = x > 5 ? "big" : "small";
    Assert.Equal("big", msg);

    var y = -3;
    var abs = y < 0 ? -y : y;
    Assert.Equal(3, abs);

    var n = 0;
    var sign = n > 0 ? "positive" : n < 0 ? "negative" : "zero";
    Assert.Equal("zero", sign);
}
