void Main() {
    var x = 10;
    x += 5;
    Assert.Equal(15, x);

    x -= 3;
    Assert.Equal(12, x);

    x *= 2;
    Assert.Equal(24, x);

    x /= 4;
    Assert.Equal(6, x);

    x %= 4;
    Assert.Equal(2, x);

    var i = 0;
    i++;
    i++;
    i--;
    Assert.Equal(1, i);
}
