// WASM platform hello-world fixture.
//
// Loaded by `demo/node/run.js` (the H4 verification harness). The
// program prints one line via Console.WriteLine, which the wasm host
// captures through the stdoutHandler callback.

namespace Wasm.Hello;

using Std.IO;

void Main() {
    Console.WriteLine("Hello, World!");
}
