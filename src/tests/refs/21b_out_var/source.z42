// Spec impl-ref-out-in-runtime: out 参数 + `out var x` 内联声明
// 期望：TryParse 写入 v=42，输出 42。

bool TryParse(string s, out int v) {
    v = 42;
    return true;
}

void Main() {
    if (TryParse("x", out var n)) {
        Console.WriteLine(n);
    }
}
