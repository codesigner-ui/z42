// Generic-T field initialized in ctor body via default(T).
// new Box<int>() → x=0; new Box<string>() → x=null.

class Box<T> {
    public T x;
    public Box() {
        this.x = default(T);
    }
    public bool IsXZero(int probe) {
        return this.x == probe;
    }
}

void Main() {
    var bi = new Box<int>();
    Assert.True(bi.IsXZero(0));
    var bs = new Box<string>();
    Assert.Null(bs.x);
}
