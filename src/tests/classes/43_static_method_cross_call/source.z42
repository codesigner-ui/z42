namespace Demo;
using Std.IO;

class MathHelper {
    static int Square(int x) { return x * x; }
    static int SumOfSquares(int a, int b) { return Square(a) + Square(b); }
}

void Main() {
    Console.WriteLine(MathHelper.Square(5));
    Console.WriteLine(MathHelper.SumOfSquares(3, 4));
}
