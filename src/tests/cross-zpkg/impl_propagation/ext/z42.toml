[project]
name    = "demo.greeter"
version = "0.1.0"
kind    = "lib"
pack    = true        # required for cross-zpkg TSIG-based reference compilation

[sources]
include = ["src/**/*.z42"]

[dependencies]
"demo.target" = "0.1.0"
