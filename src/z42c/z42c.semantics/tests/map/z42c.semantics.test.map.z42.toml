# z42c.semantics StrMap 单测（port-z42c-semantics 1A-2）。
[project]
name    = "z42c.semantics.test.map"
version = "0.1.0"
kind    = "lib"

[sources]
include = ["**/*.z42"]

[dependencies]
"z42c.semantics" = "0.1.0"
"z42c.core"      = "0.1.0"
