# z42c.project ManifestLoader 单测（port-z42c-project increment 1）。
[project]
name    = "z42c.project.test.manifest"
version = "0.1.0"
kind    = "lib"

[sources]
include = ["**/*.z42"]

[dependencies]
"z42c.project" = "0.1.0"
"z42c.core"    = "0.1.0"
