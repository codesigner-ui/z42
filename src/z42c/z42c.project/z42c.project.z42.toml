# z42c.project — 镜像 C# z42.Project（.z42.toml 解析 / 源文件发现 / zpkg builder）。
# increment 1：清单模型 + 单清单加载器（用 stdlib Std.Toml）。依赖 z42c.ir（与 C# 一致）+ z42.toml。
[project]
name    = "z42c.project"
version = "0.1.0"
kind    = "lib"

[dependencies]
"z42c.ir"  = "0.1.0"
"z42.toml" = "0.1.0"
