// z42c.project 骨架（B0）。占位 —— manifest reader / 源发现 / zpkg builder 由
// 0.3.4 project spec 落地。引用 z42c.ir 验证跨包编译。
namespace Z42.Project;

using Z42.IR;

public sealed class ProjectSkeleton {
    public string Name() {
        var ir = new IrSkeleton();
        return "z42c.project(" + ir.Name() + ")";
    }
}
