# z42c.syntax 语句解析单测（port-z42c-syntax increment 3）。
[project]
name    = "z42c.syntax.test.stmt"
version = "0.1.0"
kind    = "lib"

[sources]
include = ["**/*.z42"]

[dependencies]
"z42c.syntax" = "0.1.0"
"z42c.core"   = "0.1.0"
