# z42c.syntax Parser 单测（port-z42c-syntax increment 2）。
[project]
name    = "z42c.syntax.test.parser"
version = "0.1.0"
kind    = "lib"

[sources]
include = ["**/*.z42"]

[dependencies]
"z42c.syntax" = "0.1.0"
"z42c.core"   = "0.1.0"

[build]
# Build output → artifacts mirror (mirror-build-output-per-component, 2026-06-16):
# src/z42c/z42c.syntax/tests/parser → artifacts/build/z42c/z42c.syntax/tests/parser (no src pollution).
output_dir = "../../../../../artifacts/build/z42c/z42c.syntax/tests/parser"
