# z42c.syntax Parser 单测（port-z42c-syntax increment 2）。
[project]
name    = "z42c.syntax.test.parser"
version = "0.1.0"
kind    = "lib"

[sources]
include = ["**/*.z42"]

[dependencies]
"z42c.syntax" = "0.1.0"
"z42c.core"   = "0.1.0"
