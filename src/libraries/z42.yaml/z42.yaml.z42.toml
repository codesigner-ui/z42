[project]
name    = "z42.yaml"
version = "0.1.0"
kind    = "lib"

[dependencies]
"z42.core" = "0.1.0"
"z42.io"   = "0.1.0"   # Stream overloads on Parse / WriteTo (StreamReader / StreamWriter)
"z42.time" = "0.1.0"   # DateTime for yaml-future-timestamps (2026-05-27)

[tests.dependencies]
"z42.test" = "0.1.0"
