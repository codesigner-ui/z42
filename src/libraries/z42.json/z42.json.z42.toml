[project]
name    = "z42.json"
version = "0.1.0"
kind    = "lib"

[dependencies]
"z42.core" = "0.1.0"
"z42.text" = "0.1.0"
"z42.io"   = "0.1.0"   # Stream overloads on Parse / WriteTo (StreamReader / StreamWriter)
