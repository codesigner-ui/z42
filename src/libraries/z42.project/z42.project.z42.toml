[project]
name    = "z42.project"
version = "0.1.0"
kind    = "lib"

# namespace Z42.Build.Project —— z42 构建体系的「项目/清单」层（与 z42c 内部
# z42c.project 的 Z42.Project 区分）。z42b 与未来 z42c 重构都引用本库。
[dependencies]
"z42.core" = "0.1.0"
"z42.io"   = "0.1.0"   # Path / File / Directory / Platform
"z42.toml" = "0.1.0"   # ManifestLoader parses z42.toml
