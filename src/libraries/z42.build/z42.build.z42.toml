[project]
name    = "z42.build"
version = "0.1.0"
kind    = "lib"

# namespace Z42.Build —— z42 构建体系的「管线/编译器抽象」层（Pipeline / ICompiler /
# WorkloadBase）。依赖 z42.project（Z42.Build.Project，清单层）。
[dependencies]
"z42.core"    = "0.1.0"
"z42.io"      = "0.1.0"   # Path / File / Directory / Console
"z42.project" = "0.1.0"   # Z42.Build.Project: ProjectManifest / Target / Dirs
