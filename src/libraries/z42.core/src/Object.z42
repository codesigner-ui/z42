namespace z42.core;

// Base object protocol — every z42 type implicitly inherits these members.
// The implementations here are stubs; the VM provides the real behavior
// for primitive types. User-defined types may override them.
public class Object {
    public virtual string ToString() => "";
    public virtual bool Equals(Object other) => false;
    public virtual int GetHashCode() => 0;
}
