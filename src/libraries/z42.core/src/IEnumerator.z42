namespace Std;

// Forward iterator contract (C# 风格).
//
// Pattern:
//     while (it.MoveNext()) {
//         var x = it.Current;
//         ...
//     }
//     it.Dispose();
//
// Inherits IDisposable so foreach 升级路径（未来 codegen）可统一调
// Dispose 释放迭代状态。
public interface IEnumerator<T> : IDisposable {
    bool MoveNext();
    T Current { get; }
}
